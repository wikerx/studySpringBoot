#数据库配置

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/test
    username: root
    password: test
    driver-class-name: com.mysql.jdbc.Driver

jedis :
  pool :
    host : 127.0.0.1
    port : 6379
    password: zaq123ZMT
    config :
      maxTotal: 100
      maxIdle: 10
      maxWaitMillis : 100000
